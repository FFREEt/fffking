class Solution {
public:
    bool checkOnesSegment(string s) {
        int n=s.size();
        int i=0;
        while(s[i]=='1' && i<n)
        i++;
        while ( s[i]=='0' && i<n)
        i++;
        return i==n;
    }
};
